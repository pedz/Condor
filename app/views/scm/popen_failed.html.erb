<!-- views/scm/popen_failed.html.erb -->
<!-- called with @exception set -->
<p>
  The command:
  <pre>
    <%= h @exception.cmd.gsub(/  +/, ' ') %>
  </pre>
  failed with exit status of <%= @exception.exit_status.exitstatus %><br />
  Standard Error was:
  <pre>
  <% @exception.stderr.each do |line| -%>
    <%= h line.chomp %>
  <% end -%>
  </pre>
  <% if (md = /0010-057 Login (\S+) on (\S+)/.match(@exception.stderr[0])) %>
    The command: <br />
    <%= "Host -create #{md[1]}@#{md[2]}"%>
    <br />
    will provide the access Condor is needing for.
  <% end %>
</p>
