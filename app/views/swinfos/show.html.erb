<!-- app/views/swinfos/show.html.erb -->
<table>
  <thead>
    <tr>
      <th>Cmds</th>
      <th>Defect</th>
      <th>APAR</th>
      <th>PTF</th>
      <th>Abstract</th>
      <th>LPP</th>
      <th>VRMF</th>
      <th>Release</th>
    </tr>
  </thead>
  <tbody>
    <%- @items.each do |item| -%>
    <tr>
      <td class='command-list-td'>
	<span class='command-list-span'>
	  List
	</span>
	<ul class='command-list'>
	  <li>
	    <%= link_to "View Defect", defect_path(item.defect) %>
	  </li>
	  <li>
	    <%= link_to "Defect Changes", changes_path(item.defect) %>
	  </li>
	  <li>
	    <%= link_to "Fileset info", fileset_path(item.fileset_id) %>
	  </li>
	</ul>
      </td>
      <td><%= link_to item.defect, swinfos_path(item.defect) %></td>
      <td><%= link_to item.apar, swinfos_path(item.apar) %></td>
      <td><%= link_to item.ptf, swinfos_path(item.ptf) %></td>
      <td><%= h item.abstract %></td>
      <td><%= link_to item.lpp, swinfos_path(item.lpp) %></td>
      <td><%= link_to item.vrmf, swinfos_path("#{item.lpp} #{item.vrmf}") %></td>
      <td><%= h item.release %></td>
    </tr>
    <%- end -%>
  </tbody>
</table>
Instructions:
<ul>
  <li>Click on defect, apar, ptf, lpp, or vrmf to surf around within
  "swinfo" data</li>
  <li>Click on List to see a list of other links</li>
  <ul>
    <li>View Defect takes you to a page with the defect, etc</li>
    <li>Defect Changes takes you to a page with the change for the
    defect</li>
    <li>Fileset info provides where this fileset shipped and what it
    shipped</li>
  </ul>
</ul>
