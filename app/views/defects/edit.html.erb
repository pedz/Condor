<h1>Editing defect</h1>

<%= error_messages_for :defect %>

<% form_for(@defect) do |f| %>
  <p>
    <b>Name</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @defect %> |
<%= link_to 'Back', defects_path %>
